<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <!-- Purple Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:background="#7C6AC7"
        android:gravity="bottom"
        android:padding="20dp">

        <TextView
            android:id="@+id/tvLandingTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Landing"
            android:textSize="28sp"
            android:textColor="@android:color/white"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- White Toolbar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="16dp"
        android:background="@android:color/white"
        android:elevation="4dp">

        <!-- tappit text -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="tappit"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="#000000" />

        <!-- Explore -->
        <ImageButton
            android:id="@+id/btnExplore"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_marginStart="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/explore_events" />

        <!-- Create Event Center Button -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center">

            <Button
                android:id="@+id/btnCreateEvent"
                android:layout_width="wrap_content"
                android:layout_height="40dp"
                android:text="Create Event"
                android:textColor="#000000"
                android:backgroundTint="#E7DDF9"
                android:paddingHorizontal="20dp"
                android:gravity="center"
                android:background="@drawable/rounded_pill_button" />
        </LinearLayout>

        <!-- Notifications -->
        <ImageButton
            android:id="@+id/btnNotifications"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_marginStart="8dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/notification_icon" />

        <!-- Profile -->
        <ImageView
            android:id="@+id/btnProfile"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:layout_marginStart="8dp"
            android:src="@drawable/profile_icon" />
    </LinearLayout>

    <!-- Scroll Content -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="20dp">

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Section title -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Events"
                android:textColor="#000000"
                android:textSize="22sp"
                android:textStyle="bold"
                android:layout_marginBottom="20dp" />

            <!-- Upcoming / Past Buttons -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="48dp"
                android:background="@drawable/white_box"
                android:gravity="center"
                android:padding="4dp"
                android:layout_marginBottom="24dp">

                <Button
                    android:id="@+id/btnUpcoming"
                    android:layout_width="120dp"
                    android:layout_height="match_parent"
                    android:text="Upcoming"
                    android:textColor="#000000"
                    android:backgroundTint="#E7DDF9"
                    android:background="@drawable/rounded_left_tab" />

                <Button
                    android:id="@+id/btnPast"
                    android:layout_width="120dp"
                    android:layout_height="match_parent"
                    android:text="Past"
                    android:textColor="#000000"
                    android:background="@drawable/rounded_right_tab" />
            </LinearLayout>

            <!-- RecyclerView for Events -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvEvents"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp" />

            <!-- Empty State -->
            <LinearLayout
                android:id="@+id/emptyStateLayout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="center"
                android:padding="20dp"
                android:visibility="gone">

                <ImageView
                    android:layout_width="120dp"
                    android:layout_height="120dp"
                    android:src="@drawable/empty_calender"
                    android:alpha="0.4" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="No Events"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    android:textColor="#777777"
                    android:layout_marginTop="16dp" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="This calendar has no upcoming events."
                    android:textSize="14sp"
                    android:textColor="#999999"
                    android:layout_marginTop="6dp" />
            </LinearLayout>

        </LinearLayout>
    </ScrollView>

    <!-- Floating Add Event Button -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btnAddEvent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:layout_margin="20dp"
        app:srcCompat="@drawable/ic_add" />

</LinearLayout>
